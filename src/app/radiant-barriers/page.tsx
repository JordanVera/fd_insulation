import { ServicePage, serviceMetadata } from '@/components/service-page';

export const metadata = serviceMetadata('radiant-barriers');

export default function RadiantBarriersPage() {
  return <ServicePage slug="radiant-barriers" />;
}

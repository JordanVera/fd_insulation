import { ServicePage, serviceMetadata } from '@/components/service-page';

export const metadata = serviceMetadata('duct-sealing');

export default function DuctSealingPage() {
  return <ServicePage slug="duct-sealing" />;
}

import { ServicePage, serviceMetadata } from '@/components/service-page';

export const metadata = serviceMetadata('how-insulation-works');

export default function HowInsulationWorksPage() {
  return <ServicePage slug="how-insulation-works" />;
}

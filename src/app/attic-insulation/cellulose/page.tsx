import { ServicePage, serviceMetadata } from '@/components/service-page';

export const metadata = serviceMetadata('cellulose');

export default function CelluloseInsulationPage() {
  return <ServicePage slug="cellulose" />;
}
